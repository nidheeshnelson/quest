/* 31. SELECT * FROM 'Country'
--output of this query would be.
A). Throw error
B). Select all data from country table
C). Country
D). Throw erro  */
use companypraject;
select * from 'Country';
/*ans Throw error */
